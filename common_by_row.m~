function y = common_by_row(x)
[rows, cols] = size(x);
if rows == 0 || cols == 0
    y = [];
    return
    error('zero in matrix dimensions, Exiting...')
match = unique(x(1, :));
for i=2:length(x)
    match = intersect(match, x(i, :));
end

y = match;
end