function b = makingChange(a)
 available = [100 50 20 10 5 2 1 0.5 0.25 0.1 0.05 0.01];
 b = zeros(1, length(available));
 
 for index=1:length(available)
     b(index) = fix(a/available(index));
     a = rem(a, available(index));
 end
 
end